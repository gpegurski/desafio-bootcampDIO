A estrutura de dados é uma estrutura organizada na memória de um computador ou dispositivo de armazenamento, de forma que os dados possam ser usados de forma correta. As principais estruturas de dados são:



- **Vetores e matrizes [arrays]**: o Vetor é uma estrutura indexada. A Matriz ou Array multi-dimensional é um vetor de vetores, um vetor com duas ou mais dimensões;

- **Registro**: fornece um formato especializado para armazenar informações em memória, diferente dos arrays. Toda estrutura de registro tem um nome e seus campos podem ser acessados pelo operador (.) ponto.

- **Lista**: armazena dados de um determinado tipo, em uma ordem específica, e a diferença para o array é que essa lista tem tamanho ajustável, e com dois tipos de lista:

- **Pilha**: serve como coleção de elementos e permite acesso a somente um item de dados armazenado. Como o acesso é restrito, só pode ser lido ou removido uma única vez.

- **LIFO**: do inglês "last in, first out" que traduzindo o define como: último que entra, primeiro que sai. E é exatamente isso. Possui apenas uma entrada chamada de topo, por onde os dados entram e saem.

- **FIFO**: uma sigla assim como a anterior, mas com o significado: primeiro que entra, primeiro que sai (ou "first in, first out").

- **Filas**: aceita a remoção e inserção de elementos, seguindo o conceito FIFO, o elemento que pode ser removido é o que está na estrutura há mais tempo. Remove do início e insere no fim.

- **Árvore**: organiza de forma hierárquica, consiste em raiz e seus subordinados. Busca primeiro na raiz e após, em suas "sub-árvores".

- **Tabela HASH**: definido como <u>dispersão</u> ou <u>espalhamento</u>, associa chaves de pesquisa à valores.

- **Grafos**: são estruturas para programar relações entre objetos.

  